#include<stdio.h>

    int main()
    {
        int radius;
        float pi=3.14;
        printf("enter the radius of circle:");
        scanf("%d",&radius);
        printf("the area of circle is %f",pi*radius*radius);
        return 0;
    }
