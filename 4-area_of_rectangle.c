#include<stdio.h>

    int main()
    {
        int length,bredth;
        printf("enter the length of rectangle:");
        scanf("%d",&length);
        printf("enter the bredth of rectangle:");
        scanf("%d",&bredth);
        printf("the area of rectangke is %d:",length*bredth);
        return 0;
    }